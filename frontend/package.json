{
    "name": "frontend",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "deploy": "gh-pages -d dist"
    },
    "devDependencies": {
        "@types/telegram-web-app": "^7.10.1",
        "gh-pages": "^6.3.0",
        "typescript": "~5.8.3",
        "vite": "^7.0.0"
    },
    "dependencies": {
        "zustand": "^5.0.8"
    }
}
